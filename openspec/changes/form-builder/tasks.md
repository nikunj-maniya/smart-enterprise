## 1. Builder — Fields

- [x] 1.1 Form CRUD APIs: create/list/read drafts, save draft field-set, guardrail validation, audit-logged _(Slice 1)_ — `apps/api/prisma/schema.prisma` + migration `20260708053955_form_definition_timestamps` add `createdAt`/`updatedAt` to `FormDefinition`; `modules/forms/` extended with `GET/POST /forms/drafts` + `GET/PUT /forms/drafts/:key` (Enterprise Admin, tenant-scoped, registered before `/:key`); guardrails via `saveDraftFieldsRequestSchema` (§6.3 type enum, unique keys, non-empty label) and `createFormDraftRequestSchema` (slugified key, 409 on collision); `create_draft`/`save_draft_fields` audit rows written. Verified end-to-end live against Postgres incl. tenant isolation, RBAC 401/403, and 404 route-ordering
- [x] 1.2 Build the two-pane builder per the design (form list with badges, field rows with drag handle, Required toggle, edit/delete) _(Slice 1)_ — `pages/organization/FormBuilder.tsx` (list pane from `GET /forms/drafts` with fieldCount/updated/Draft-Published badge; field-editor pane with `grip-vertical` drag handle, native HTML5 DnD reorder, clickable Required/Optional pill, edit/delete), route `/organization/form-builder` gated `RequireRole(EnterpriseAdmin)` + sidebar entry added. Published/core forms render read-only pending Slice 3/7 draft-start
- [x] 1.3 Add/Edit Field modal (label, §6.3 type select, required) wired to draft save _(Slice 1)_ — `components/form-engine/FieldModal.tsx`: label input, §6.3 `FIELD_TYPE_OPTIONS` select (excludes deferred `signature`), Required `Switch`; `onSave` merges into `FormBuilder.tsx`'s field list and persists via the same `PUT /forms/drafts/:key`, preserving untouched field properties (options/validation/visibilityRule) on edit
- [x] 1.4 Field visibility-rule editing (show-when conditions referencing other fields) _(Slice 2)_ — `components/form-engine/FieldModal.tsx` adds a Visibility section: add/remove "show when" condition rows against any other field (`FormBuilder.tsx` excludes self via `otherFields`), full `RuleOp` set with values coerced to the referenced field's type, serializing to the shared `VisibilityRule` grammar and persisted via the existing `PUT /forms/drafts/:key`. QA caught a data-loss bug (untouched OR/nested-AND rules silently collapsed to `undefined` on any unrelated field edit); fixed via `isRuleFullyRepresentable`/`preservedRule`/`ruleTouched` so a rule this flat-AND editor can't fully display is preserved verbatim until the admin explicitly edits Visibility, with a warning banner shown meanwhile.
  - Follow-up fixes for QA's remaining risks: (1) `collectRuleFields` (`shared/form-engine/rules.ts`) plus a `saveDraftFieldsRequestSchema` refinement reject a saved field-set if any visibility rule references a field key not on the form; `FormBuilder.tsx`'s delete button is disabled with an explanatory tooltip while another field's rule still references it, so the dangling reference can no longer happen from the UI. (2) `FieldModal.tsx`'s `handleSave` now blocks saving (inline error) instead of silently dropping a condition row missing a required value. (3) Condition rows for `date`/`datetime`/`time`-typed referenced fields render a native `date`/`datetime-local`/`time` input instead of free text. (4) The three condition-row selects get per-row `aria-label`s. (5) Manually verified live end-to-end in the browser (create draft → add checkbox+dependent field → confirm delete blocked with tooltip → remove condition → delete succeeds → blank-value save blocked → date-typed condition renders a native picker → all persisted correctly across a reload).

## 2. Publish & Versioning

- [x] 2.1 Publish API: validate guardrails, create immutable version, expose to New Request; edits after publish start a new draft _(Slice 3)_ — `modules/forms/forms.service.ts` adds `publishDraft` (re-validates via the shared engine's `parseDefinition`, flips the draft row's status to `published` in place, audit-logs `publish`) and `startFormDraft` (idempotent; clones the latest published row into a new `version + 1` draft, leaves the published row untouched, blocks core forms with 400); routes `POST /forms/drafts/:key/publish` and `POST /forms/drafts/:key/start` registered ahead of `/:key`. Verified live against Postgres: publish flips status and surfaces in `GET /forms`, re-publish with no draft 404s, start-draft clones a new immutable version leaving v1 untouched and is idempotent, core forms rejected
  - Follow-up fix for QA's risks: (1) `publishDraft` now rejects publishing a field-less draft with a 400 (`Add at least one field before publishing this form`); the FormBuilder Publish button is correspondingly disabled with a tooltip while the form has zero fields. (2) RBAC 403 on both new routes (`/publish`, `/start`) independently re-verified live for a non-admin account. (3) The `alice@acme.test` QA account, left on a weak known password during the Slice 3 QA pass, was rotated to a strong random password (not recorded, `mustChangePassword=true`). Re-verified live: empty-publish → 400, non-admin publish/start → 403, add-field-then-publish → 200.
- [x] 2.2 Draft/Published badges + Save Draft / Publish buttons per the design _(Slice 3)_ — `pages/organization/FormBuilder.tsx`: enabled `Publish` (calls `POST /forms/drafts/:key/publish`, refreshes list/badge), replaced the old "coming in a later release" copy for published non-core forms with an `Edit` button wired to `POST /forms/drafts/:key/start`; core forms keep a static read-only message pending Slice 7. Added `publishing`/`startingDraft` state to prevent action races. Publish button additionally disabled (with tooltip) when the draft has no fields, mirroring the server-side min-field guardrail; verified in the browser (empty draft → disabled, after adding a field → enabled)

## 3. Generic Renderer

- [x] 3.1 Generic renderer component: all §6.3 field types + client-side visibility/validation from metadata _(Slice 4)_ — the `FormRenderer.tsx`/`useFormEngine.ts`/`fields/*` core-renderer (all 14 value-bearing §6.3 types in `FIELD_COMPONENTS`, `consent-link`→`CheckboxField`, `signature`/`file-upload` as never-required disabled stubs, `section`/`group` as layout-only no-renders) landed in Slice 7-8; this slice added the employee-facing path: `lib/api.ts` `listPublishedForms()`/`getPublishedForm(key)`, `components/form-engine/definition.ts` `definitionFromDto()` (null→undefined coercion for the wire DTO's optional JSON columns), and `pages/requests/NewRequest.tsx` (published-form card grid, loading/empty/error states) + `pages/requests/RequestForm.tsx` (loads/converts a definition, drives `useFormEngine`+`FormRenderer` with metadata-driven client-side visibility/validation and hidden⇒absent clearing, submit wiring with per-field server-400 feedback + inline success card). Routes `/requests/new` + `/requests/new/:key` registered in `App.tsx` inside authenticated `AppShell` (no role gate); `MemberHome` turned into a "Start a new request" entry point so employees can reach it. Placed under new `pages/requests/` (employee-facing) rather than the admin-only `pages/organization/`. Typecheck + build clean
- [x] 3.2 Generic submission endpoint: server re-validation against the pinned version, standard request creation _(Slice 4)_ — the generic path (`POST /requests` → `requests.controller.create` → `requests.service.createRequest`, renderer-agnostic) already pinned the tenant's latest published version and re-validated server-side via the shared `validatePayload` (required, types, option-membership, hidden⇒absent rejection), creating the standard request + first status-history row + audit log in one tenant-scoped transaction. The one real gap: custom (builder-created) forms carry no status model (config is Slice 6), so submission `400`ed — fixed by adding `DEFAULT_GENERIC_INITIAL_STATUS = 'Submitted'` (`shared/form-engine/status-model.ts`) and falling back to it in `getPublishedDefinitionForSubmission` (removing the throw) so custom forms are submittable; core forms still use their seeded `states[0]`. `transitions.service.ts`'s own no-status-model guard left untouched (approvals are Slice 5). Shared engine tests (8/8, incl. hidden-field-value and missing-conditionally-required rejection) directly cover the acceptance scenarios
- [x] 3.3 Custom-form requests flow through approvals queue, My Requests, notifications, audit _(Slice 5)_ — DB indexes (`Request.requesterId`, `RequestApprover(approverId, decision)`); shared `approverRuleSchema`/`stageRulesSchema`, a discriminated-union `notificationSchema` (`request_approved`/`rejected`/`needs_approval`/`status_changed`/`approval_reminder`), `RequestListItemDto`/`ApprovalQueueItemDto`; backend submission-time approver snapshotting (`approver-resolution.ts`) into `RequestApprover`, `request_needs_approval` notifications to the snapshotted approvers, `GET /requests` + `GET /requests/approvals`, and parallel-approver reconciliation on Approved/Rejected; web `MyRequests`/`ApprovalsQueue` pages with approve/reject wiring, Topbar notification rendering, an accessibility pass on `Overlay`/notification rows/toggles.
  - Follow-up fix for the initial slice-runner QA FAIL: Withdraw and pre-approval Cancel don't produce a real approved/rejected decision to attribute to the snapshotted approvers (they never acted), and `RequestApprover.decision` is a strict `pending|approved|rejected` enum depended on by 3 files — so writing a fake decision would corrupt the audit trail. Fixed instead in `requests.service.ts`'s `listApprovalQueue`: a `pending` row is only "awaiting" when the request has never transitioned since its approvers were snapshotted (no `RequestStatusHistory` row with a non-null `fromState` — every request's creation-time row has `fromState: null`, every later transition writes a real one). Verified live against Postgres: submit → approver sees it in `pending` (`awaitingCount: 1`) → requester withdraws → approver's `pending` tab is empty (`awaitingCount: 0`, not misfiled into `decided` either); regression-checked the existing Approve happy path is unaffected.

## 4. Routing & Status Config

- [x] 4.1 Builder UI + API for approval routing config (role-restricted pickers, parallel mode) _(Slice 6)_ — shared `isPickerFieldType`/`validateStageRules` (`packages/shared/src/form-engine/metadata.ts`) and `saveDraftRoutingRequestSchema`; `PUT /forms/drafts/:key/routing` (tenant-scoped, admin-only, audit-logged `save_draft_routing`) validates each approver rule's field exists on the form and is a `user-picker`/`project-picker` type before persisting `ApprovalWorkflow`; `saveDraftFields` and `publishDraft` both re-run `validateStageRules` against the current field set so deleting/retyping a field a routing stage depends on is rejected (`400` naming the field) rather than silently orphaning the stage. Frontend: new `RoutingEditor.tsx` (approver-stage picker-field select + optional single-condition visibility gate) wired into a "Routing" tab in `FormBuilder.tsx`; `FieldModal` gained a "Restrict to roles" control for picker fields (persists into existing `PickerConfig.roles`) and disables the Field type select (with explanatory note) when the field is referenced by a routing stage, mirroring the pre-existing delete guard.
- [x] 4.2 Builder UI + API for status-model config with state-machine validation _(Slice 6)_ — shared `validateStatusModel` guardrail (`packages/shared/src/form-engine/status-model.ts`: >=1 terminal state, no orphan/unreachable states via BFS, no self-approval) and a structural-only `saveDraftStatusModelRequestSchema` (guardrails deliberately not baked into the save-time schema, so work-in-progress models can be saved); `PUT /forms/drafts/:key/status-model` persists states/transitions as a plain upsert (tenant-scoped, admin-only, audit-logged `save_draft_status_model`), while `publishDraft()` runs `validateStatusModel` and `400`s naming the specific problem (e.g. `State "Stranded" is unreachable from the initial state "Submitted"`). Frontend: new `StatusModelEditor.tsx` (states list with drag-reorder/add/remove, role-gated from/to transition editor, rename-cascades-into-transitions, client-side guardrail preview before save) wired into a "Status Model" tab in `FormBuilder.tsx`; publish-time guardrail errors surface through the existing save-error banner. 14/14 shared `form-engine` tests pass (incl. terminal/orphan/self-approval/requester-alone scenarios); live-verified against Postgres (routing 400s on non-picker/unknown field, status-model WIP-with-orphan saves but publish 400s and then succeeds once fixed, self-approval 400s at publish, RBAC 403s for non-admins).

## 5. Core-Form Editing

- [x] 5.1 Open core forms in the builder; allow relabel/reorder/validation/routing edits; block structural edits with explanation _(Slice 7)_ — shared `validateCoreFormFieldEdit` (`packages/shared/src/form-engine/metadata.ts`) diffs a core form's field set by key/type, allowing relabel/reorder/required/validation edits and flagging added/removed/retyped fields (4 new tests, 21/21 shared tests pass). `modules/forms/forms.service.ts`: `startFormDraft` no longer blocks core forms (clones the published row-set same as custom forms); `saveDraftFields` gates a structural-diff check on `draft.renderer === 'core'` and 400s naming the problem (`Core forms can't add new fields: <keys>` / `...can't remove fields: <keys>` / `...can't change field type: <keys>`); routing/status-model saves are left unguarded since those edits are explicitly allowed. `pages/organization/FormBuilder.tsx` unifies the published-form banner for core and custom forms (Edit button starts a draft) with a new banner while a core draft is open; Add Field and Delete Field are disabled with an explanatory tooltip for core-form drafts; `FieldModal.tsx`'s field-type select is likewise disabled+tooltipped whenever `isCoreForm`. Live-verified against Postgres on the `leave` core form: draft-start clones v1, relabel/reorder 200 and persist, add/remove/retype each 400 with the specific field named, a custom form's add/retype are unaffected, and publish+`GET /forms/leave` immediately serves the reordered v2 without redeploy.
  - Non-blocking follow-up noted by QA: `saveDraftFields`'s inline diff logic is equivalent to but duplicates `validateCoreFormFieldEdit` rather than calling it directly — recommend a small follow-up to route through the shared function as the single source of truth.

## 6. Verify

- [x] 6.1 §16 acceptance: admin builds a new conditional form, publishes, employee submits, approver approves — zero code changes — live-verified end-to-end against Postgres on a fresh isolated tenant: built a form with a `region` single-select, a `when region == APAC` conditional textarea, and a `user-picker` approver field; configured routing (field-sourced approver) and a status model gated to a custom role; published; the employee immediately saw it in `GET /forms` with no code changes; submitted with `region: APAC` (surfacing the conditional field); the approver saw it in `GET /requests/approvals` and approved via `POST /requests/:id/transitions`, moving the request to `Approved`
- [x] 6.2 Republish leaves in-flight requests on their pinned version — live-verified: submitted an in-flight request against v1 (custom-role-gated status model), then started a new draft, changed the status model to require a different role (`enterprise-admin`), and published v2. The v1-pinned in-flight request still approved successfully under the original custom role (`Request.formDefinitionId` is a hard FK to the specific published row, never a dynamic key+version lookup), while a new request submitted after the republish correctly rejected that same custom-role approver with a 403 — confirming genuine per-request version isolation, not just an unenforced check
- [x] 6.3 Hidden-field payload value and missing required field both rejected server-side — live-verified via raw API calls (no client involved): submitting a value for the conditional field while its gate was false (`region: EMEA` with `escalation_reason` set) 400'd with "This field is not applicable and must not be submitted"; omitting the same field while its gate was true (`region: APAC`, `escalation_reason` absent) 400'd with "This field is required"
- [x] 6.4 Invalid status model (orphan state) refused at publish with a clear message — live-verified: added an `states: [...,"Stranded"]` state with no incoming/outgoing transitions to the draft status model and attempted to publish; refused with a 400 naming the exact problem: `State "Stranded" is unreachable from the initial state "Submitted"`
- [x] 6.5 Core Leave form relabel publishes and renders without redeploy — live-verified: started a draft from the tenant's published core Leave form, relabeled the `context` field, published as v2, and immediately re-fetched `GET /forms/leave` (both as admin and as a plain employee) — the new label served instantly against the same running process, no restart
