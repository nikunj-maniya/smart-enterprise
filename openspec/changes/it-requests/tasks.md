## 1. Item Catalog

- [x] 1.1 `ItemCatalog` CRUD API: add/rename/archive, delete blocked when referenced, audit-logged, seeded on tenant activation _(Slice 1)_ — new `ItemCatalog` table (migration `20260710122150_it_requests_catalog_and_assignee`), `item-catalog.service.ts` (`GET/POST /item-catalog`, `PUT/DELETE /item-catalog/:id`, Enterprise Admin only), seeded from the exact PRD lists (13 software, 27 hardware) the IT form used to embed statically — `SOFTWARE_ITEMS`/`HARDWARE_ITEMS` exported from `core-forms.ts` rather than duplicated. "Referenced" is computed via a JSON-path `array_contains` query against submitted requests' payloads (no FK — items are named by string, not id). Verified live: a fresh tenant seeds 13/27 correctly; adding "YubiKey" appears immediately in the form; deleting a referenced item is refused (409) with an archive-instead message; archiving succeeds.
- [x] 1.2 Catalog management UI in the Enterprise Admin console (software + hardware lists, archive flow) _(Slice 1)_ — new `pages/organization/ItemCatalog.tsx`: Software/Hardware pill tabs, inline add-item row, per-row Archive/Unarchive + Delete (409 shown inline rather than pre-emptively disabling the button, since the backend is authoritative on "referenced").

## 2. IT Request Form

- [x] 2.1 Add the IT core `FormDefinition` (access-type branch visibility rules, catalog-sourced item options, impact, Process Head routing) _(Slice 2)_ — branch visibility/impact/routing pre-existed from `form-engine`. **Changed the item fields from static embedded options to a `{ source: 'item-catalog:<type>' }` marker** (design.md's actual, not-yet-implemented decision — "master data, not form-field options"): resolved live, server-side, in `forms.service.ts` (`resolveCatalogOptions`) whenever a definition is served for rendering — active-only for a fresh submission's blank form, active+archived for a specific pinned request (so a past submission's now-archived item still renders). No compile.ts or frontend renderer changes needed — the checkbox-group field receives what looks like an ordinary static options array either way.
- [x] 2.2 Submission endpoint: Zod validation, branch re-validation server-side, ≥1 item required, request created `Requested` with snapshotted approver _(Slice 2)_ — branch re-validation and ≥1-item-required already worked generically (hidden⇒absent; an empty array counts as empty). **Found and fixed the same vestigial-pre-state bug as Visitor** (`Requested` never advancing to `Pending Approval`) — already covered by the general fix built during `visitor-management` (`forms.service.ts`'s `resolveEffectiveInitialStatus`), so IT needed no additional code here, just this live confirmation. Added the one genuinely new check: `it-catalog-rules.ts`'s `assertItemsInActiveCatalog` — the authoritative membership check for submitted item names (catalog-sourced options aren't statically validated at compile time, unlike a normal static list). Verified live: cross-branch fields refused (400); submission lands directly in `Pending Approval`.
- [ ] 2.3 Build the IT form screen per the design (access-type pills → progressive disclosure, item chips, impact pills) _(Slice 2)_ — **scope reduction, not built**: same as `leave-wfh-requests`/`visitor-management`, the existing generic `RequestForm`/`FormRenderer` renders the IT form as-is (branch visibility, catalog-sourced items, impact pills all work — verified live), not the design's bespoke progressive-disclosure layout.
- [x] 2.4 Wire Process Head approval: approve → `Approved` (enters queue), reject with reason → `Rejected`, requester notified _(Slice 3)_ — already worked via the generic decision engine once 2.2's status-model fix landed; IT's status model already had a real `Rejected` state (unlike Visitor), so the `approval-workflow`/`visitor-management` reject-outcome-name generalization applies with no further change needed here. Verified live via the approve path; reject not independently re-tested this session (identical mechanism to the already-verified Visitor/Leave/WFH reject paths).

## 3. Fulfilment Queue

- [x] 3.1 Fulfilment APIs: queue query (IT Admin only), assign-to-me (guarded claim), start (`Approved → In Progress`), hand-over (`In Progress → Fulfilled`), all audit-logged _(Slice 4)_ — new `Request.itAssigneeId` promoted column (same migration as 1.1) + `fulfilment.service.ts` (`GET /requests/fulfilment-queue`, `POST /requests/:id/claim`) + a new `requireItAdmin` middleware. Start/hand-over ride the *existing* generic transition endpoint, with one addition in `transitions.service.ts`: any IT-Admin-gated transition on an IT request now also requires `request.itAssigneeId === actor.id` — claim-based ownership, not just role membership. Verified live: claim is a guarded single-row update; a second claim attempt / an unclaimed Start / a wrong-admin Start are all refused with the right status codes (409/403).
- [x] 3.2 Build the Fulfilment Queue per the design (stat cards, Open/Fulfilled tabs, impact badges, assignee, lifecycle buttons, empty state) _(Slice 4)_ — new `pages/requests/FulfilmentQueue.tsx`, modeled on `FrontDesk.tsx`'s structure (stat cards, pill tabs, card actions).
- [x] 3.3 Requester notifications on In Progress and Fulfilled _(Slice 5)_ — already covered generically (`notifyRequesterOfTransition` fires `request_status_changed` for any non-decision-outcome transition); no new code needed. Verified live: requester notified at Approved, In Progress, and Fulfilled.

## 4. Verify

- [x] 4.1 Software and hardware branches each submit cleanly; cross-branch fields rejected server-side — verified live (see 2.2).
- [x] 4.2 Catalog archive hides an item from new requests but past requests still render it — verified live: archiving "YubiKey" removed it from a fresh form's options while the original request's own pinned definition still listed it (see 2.1).
- [x] 4.3 Approve → queue → assign → In Progress → Fulfilled, with requester notified at each stage — verified live end-to-end, full lifecycle (see 3.1/3.3).
- [x] 4.4 Hand-over refused on a never-started request; non-IT-Admin refused queue access — verified live: hand-over on a still-`Approved` (never-claimed/started) request refused (400, no such declared transition); an Enterprise Admin calling the IT Admin-only claim/queue endpoints refused (403).
- [x] 4.5 Catalog edits and every fulfilment step appear in the Audit Log — verified live: `ItemCatalog` create/update and `Request` claim/transition entries all present after exercising the full lifecycle.
